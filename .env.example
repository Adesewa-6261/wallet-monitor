# Copy to .env.local and fill in. .env.local is gitignored.

# Bitnob — app.bitnob.com → Settings → API Keys
# Note: key creation requires an IP whitelist and expiry date.
BITNOB_CLIENT_ID=
BITNOB_CLIENT_SECRET=

# CoinGecko — used only for ETH and TRX, which Bitnob does not price
COINGECKO_API_KEY=

# Postgres connection string
DATABASE_URL=

# Signs login tokens. Generate with:
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET=

# Telegram bot token, from @BotFather. The poller records transactions without
# it; alerts simply are not delivered until it is set.
TELEGRAM_BOT_TOKEN=

# Optional
# ALERT_POLL_MINUTES=2
# BITCOIN_GAP_LIMIT=20

# Shared secret the Telegram bot uses to prove itself to the API when asking
# for a token on behalf of a linked chat. Generate with:
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
BOT_SHARED_SECRET=

# Where the bot reaches the API. Defaults to the local app, which is correct
# when the bot runs in-process with it.
# API_BASE_URL=http://127.0.0.1:8000

# TronGrid, for Tron transaction history. Optional — TronGrid serves
# unauthenticated requests at a lower rate limit.
# TRONGRID_API_KEY=
